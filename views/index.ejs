<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <hr>
    <% messages.forEach(message => { %>
        <li>
            <div>
                <h3>From: <%=message.user%></h3>
                <p><%=message.text%></p>
                <p>Date: <%=message.added.getMonth()%>-<%=message.added.getDate()%>-<%=message.added.getFullYear()%></p>
            </div>
        </li>
    <% }); %>
</body>
</html>